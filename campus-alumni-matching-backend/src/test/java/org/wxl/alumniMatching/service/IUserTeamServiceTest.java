package org.wxl.alumniMatching.service;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

import javax.annotation.Resource;
import java.util.Iterator;
import java.util.Map;
import java.util.Set;

@SpringBootTest
public class IUserTeamServiceTest {
    @Resource
    private IUserTeamService userTeamService;

    @Test
    public  void getUserTags(){
//        Map<String, Integer> userTags = userTeamService.getUserTags(1L);
//
//        Iterator<Map.Entry<String, Integer>> iterator = userTags.entrySet().iterator();
//        while (iterator.hasNext()) {
//            Map.Entry<String, Integer> entry = iterator.next();
//            System.out.println(entry.getKey());
//            System.out.println(entry.getValue());
//        }
    }
}
